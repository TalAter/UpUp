// Mocking the test structure to complete suit

/*
var assert = require('assert');
describe('name of test suit', function() {
  describe('name of specific test', function() {
    it('should return something', function() {
      assert.equal(true,true);
    });
  });
});
*/
